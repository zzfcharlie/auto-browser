# auto-browser v3 修改说明

## 版本信息

- 版本：v3.0.0
- 项目：auto-browser
- 定位：基于 Chrome CDP 的浏览器自动化框架
- Node.js：>=18
- 许可证：MIT

## 核心升级

### 1. 稳定元素引用

- 页面元素增加持久化 `ref`，例如 `e18`。
- 快照元素保存 CSS selector、XPath、元素类型、文本、名称、角色、位置和状态。
- `click`、`fill`、`hover` 支持通过 `@eN` 操作。
- 快照保存到 `cache/last-snapshot.json`，不同 CLI 进程之间可以复用引用。
- 元素失效后按 CSS selector、XPath 和语义候选自动恢复。

### 2. 候选评分和安全校验

- 语义恢复不再简单选择第一个匹配节点。
- 候选元素综合 role、accessible name、文本、placeholder、父级上下文和位置评分。
- 低置信度匹配会输出 warning。
- click 会检查元素是否存在、可见和未禁用。
- fill 会检查元素是否为可编辑控件。
- hover 会检查元素是否可见和可操作。

### 3. 动作结果验证

- click 前后比较 URL、标题、页面文本、DOM 长度和对话框数量。
- 点击结果会标记为页面发生变化或没有可观察变化。
- fill 会读取真实 DOM value，确认输入确实写入。
- DOM 重建后仍可以使用旧 ref 重新定位和操作。

### 4. 导航和等待机制

- 默认导航从 `networkidle0` 改为 `domcontentloaded`。
- 现代 SPA、GitHub、Kaggle 等持续请求页面不再因为 network idle 长时间阻塞。
- 新增等待能力：
  - 等待 CSS selector
  - 等待文本
  - 等待 URL
  - 等待 DOM 稳定

CLI 示例：

```bash
node bin/cli.mjs wait selector "#app"
node bin/cli.mjs wait text "加载完成"
node bin/cli.mjs wait url "github.com"
node bin/cli.mjs wait stable
```

### 5. 表单控件支持

新增：

```bash
node bin/cli.mjs select "#country" "China"
node bin/cli.mjs check "#agree"
node bin/cli.mjs uncheck "#agree"
node bin/cli.mjs contenteditable "#editor" "内容"
```

支持原生 select、checkbox、radio 和 contenteditable，并触发 input/change 事件。

### 6. iframe 和 Shadow DOM

- 快照支持同源 iframe 内的元素。
- frame 元素使用 `f1-e1` 形式的 ref。
- 支持：

```bash
node bin/cli.mjs fill @f1-e2 "frame value"
node bin/cli.mjs click @f1-e1
```

- 支持开放式 Shadow DOM 元素解析。
- Shadow DOM 元素记录 host 和内部 selector。
- 跨域 iframe 会安全跳过，不会导致整页快照失败。

### 7. 页面快照 diff

新增 `core/diff.mjs`，可以比较前后两次快照：

- 新增元素
- 删除元素
- 文本变化
- 位置变化
- 可见性变化
- disabled 状态变化

CLI 示例：

```bash
node bin/cli.mjs snap
node bin/cli.mjs diff
```

### 8. 公共 API 扩展

`AutoBrowser` 新增：

```js
await ab.waitForSelector(selector)
await ab.waitForText(text)
await ab.waitForUrl(pattern)
await ab.waitForDomStable()
await ab.diffMaps(previousMap)
```

### 9. Daemon 改进

- back/reload 使用 `domcontentloaded`，避免持续请求页面卡住。
- 默认 CORS 从任意来源收紧为本机来源。
- 错误响应增加结构化 hint。

### 10. 自动化测试

新增：

```text
tests/smoke.mjs
```

覆盖页面地图、iframe、Shadow DOM、select、checkbox、contenteditable、等待机制和快照 diff。

运行：

```bash
npm test
```

## Kaggle 实测

v3 已在 Clash 代理和独立 Chrome CDP 环境中测试 Kaggle：

```bash
node bin/cli.mjs open https://www.kaggle.com/competitions
node bin/cli.mjs snap
node bin/cli.mjs click @e16
node bin/cli.mjs snap
```

可以识别并点击 Playground 分类，页面 URL 成功变化为带有 `hostSegmentIdFilter=8` 的筛选地址，二次快照可以看到 Playground 筛选状态。

## v3 使用示例

```bash
node bin/cli.mjs open https://www.kaggle.com/competitions
node bin/cli.mjs snap
node bin/cli.mjs click @e16
node bin/cli.mjs wait stable
node bin/cli.mjs snap
```

## 已知限制

- 跨域 iframe 当前只能跳过，不能直接操作。
- closed Shadow DOM 无法通过浏览器标准 API 读取。
- 自定义 UI 下拉框仍需要 Element Plus、Ant Design 等适配器。
- 多 tab 的快照生命周期隔离仍需继续加强。
- daemon/MCP 仍需要进一步完善 URL 白名单、eval 权限和高风险操作确认。
- 页面 diff 当前是重新建图后比较，不是实时 DOM 增量监听。

## 升级建议

```bash
npm install
npm test
```

使用前需要启动带 CDP 的 Chrome：

```powershell
Start-Process "C:\Program Files\Google\Chrome\Application\chrome.exe" `
  -ArgumentList "--remote-debugging-port=9222"
```
